{
  "name": "bitcoin-auth-test",
  "module": "src/test-auth.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:node": "npm run build && node dist/test-auth.js",
    "test:bun": "bun src/test-auth.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@bsv/sdk": "^1.5.2",
    "bitcoin-auth": "^0.0.4"
  }
}
